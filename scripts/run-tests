#!/bin/sh
set -eu

SCRIPT_DIRECTORY=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
TRANSHOOTER_ROOT=$(CDPATH= cd -- "$SCRIPT_DIRECTORY/.." && pwd)
export TRANSHOOTER_ROOT
exec "$TRANSHOOTER_ROOT/tests/failure-smoke/internal/run-tests" "$@"
