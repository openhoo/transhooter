#!/bin/sh
set -eu
. "$(dirname -- "$0")/../deploy/scripts/compose-lib.sh"
load_state
export PROVIDER_PROFILE=$profile
compose down --remove-orphans "$@"
printf 'Persistent volumes and .secrets were preserved.\n'
