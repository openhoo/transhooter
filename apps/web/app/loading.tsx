export default function Loading() {
  return (
    <section className="panel" role="status" aria-live="polite">
      <span className="status warning">Loading verified consultation data…</span>
    </section>
  );
}
